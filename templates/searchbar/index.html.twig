
{% extends 'base.html.Twig' %}
{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('assets/accueil/css/main.css') }}"/>
{% endblock %}
{% block body %}
<div id="wrapper" style="background-color:white!important;">

				<!-- Menu -->
					

				<!-- Main -->
					<div id="main">

						<!-- Post -->
							<section>
                         <article class="post">
 
					<header>			
                        <div class="title"> <h3>Résultat de la recherche : "{{ search }}" - {{ research | length }} resultat(s)</h3>	</div>
                    </header>
                    <div class="row">
                    {{dump(research)}}
                        {% for profil in research %}
                        <div class="col-2 col-6-medium col-12-small">
                                <div class="ui link cards">
                                    <div class="card">
                                        <div class="image">
                                            <img src="{{ asset(profil.link)}}" style="width: 100%; height: 150px;">
                                        </div>
                                        <div class="content">
                                            <div class="header">{{profil.name}} {{profil.first_name}}</div>
                                        </div>
                                    </div>
                                </div> 
                            
                        </div>    
        
                        {% endfor %}
                </div>
            </section>


			</div>
</article>

</div>
{% endblock %}
{% block javascripts %}

{% endblock %}