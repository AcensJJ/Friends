
{% extends 'base.html.Twig' %}

{% block stylesheets %}
	<link rel="stylesheet" href="{{ asset('assets/accueil/css/main.css') }}" />
{% endblock %}

{% block body %}

	<section class="tiless"> 
	
		{% for categorie in categories %}
		<article class="style{{i}}">						
			<span class="image">
				<img style="height: 300px; width: 475px;" src="{{ asset(categorie.image) }}" alt="" />
			</span>
			<a href="{{ path('souscategories',{"id":categorie.id})}}">
				<h2>{{ categorie.name }}</h2>
				<div class="content">
					<p>Voir cette catégorie !</p>
				</div>
			</a>
		</article>
		{% set i = i + 1 %}
		{% if i > 6 %}
		{% set i = 1 %}
		{% endif %}
		{% endfor %}			
	</section>
{% endblock %}
{% block javascripts %}


{% endblock %}
